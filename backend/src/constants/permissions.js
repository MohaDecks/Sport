const {
  ADMIN_MODULES,
  PERMISSION_GROUPS,
  PERMISSION_MENUS,
  ALL_PERMISSIONS,
  getUserPermissions,
} = require('./adminModules');

module.exports = {
  ADMIN_MODULES,
  PERMISSION_GROUPS,
  PERMISSION_MENUS,
  ALL_PERMISSIONS,
  getUserPermissions,
};
